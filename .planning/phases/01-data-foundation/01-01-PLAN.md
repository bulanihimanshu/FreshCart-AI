---
id: "01-01"
phase: 1
plan: 1
wave: 1
title: "Project Skeleton"
depends_on: []
files_modified:
  - "data/.gitkeep"
  - "data/raw/.gitkeep"
  - "data/processed/.gitkeep"
  - "data/precomputed/.gitkeep"
  - "notebooks/.gitkeep"
  - "backend/.gitkeep"
  - "backend/api/.gitkeep"
  - "backend/models/.gitkeep"
  - "backend/utils/.gitkeep"
  - "backend/saved_models/.gitkeep"
  - "backend/precomputed/.gitkeep"
  - "backend/professor_scripts/.gitkeep"
  - "frontend/.gitkeep"
  - "frontend/public/.gitkeep"
  - "frontend/src/.gitkeep"
  - "frontend/src/api/.gitkeep"
  - "frontend/src/context/.gitkeep"
  - "frontend/src/pages/.gitkeep"
  - "frontend/src/components/.gitkeep"
  - "frontend/src/components/layout/.gitkeep"
  - "frontend/src/components/shop/.gitkeep"
  - "frontend/src/components/recommendations/.gitkeep"
  - "frontend/src/components/cart/.gitkeep"
  - "frontend/src/components/settings/.gitkeep"
  - "frontend/src/hooks/.gitkeep"
  - "scripts/.gitkeep"
requirements_addressed: ["DATA-01", "DEL-02"]
autonomous: true
---

# Plan 01-01: Project Skeleton

## Objective

Scaffold the complete FreshCart AI folder structure exactly matching PRD Section 10 and Section 5 (frontend) and Section 6 (backend). All directories must exist before any other phase can write into them.

<read_first>
- `FreshCart_AI_PRD_v2.md` §5 — Frontend file/component structure (exact folder names)
- `FreshCart_AI_PRD_v2.md` §6 — Backend file structure (exact folder names)
- `FreshCart_AI_PRD_v2.md` §10 — Complete project folder structure spec
- `.planning/phases/01-data-foundation/01-CONTEXT.md` — Decisions D-02, D-03, D-04
</read_first>

## Tasks

### Task 1: Create top-level directories
<action>
Create the following top-level directories (use `mkdir -p` or equivalent). Each must contain a `.gitkeep` file so it's tracked by git even when empty:

```
data/
data/raw/
data/processed/
data/precomputed/
notebooks/
scripts/
backend/
frontend/
```

Run: `mkdir -p data/raw data/processed data/precomputed notebooks scripts backend frontend`
Then touch a `.gitkeep` in each: `for d in data/raw data/processed data/precomputed notebooks scripts backend frontend; do touch $d/.gitkeep; done`
</action>

<acceptance_criteria>
- `data/raw/` directory exists: `Test-Path "data/raw"` returns True
- `data/processed/` directory exists: `Test-Path "data/processed"` returns True
- `notebooks/` directory exists: `Test-Path "notebooks"` returns True
- `scripts/` directory exists: `Test-Path "scripts"` returns True
- `backend/` directory exists: `Test-Path "backend"` returns True
- `frontend/` directory exists: `Test-Path "frontend"` returns True
</acceptance_criteria>

---

### Task 2: Create backend sub-directories
<action>
Create all backend sub-directories from PRD Section 6 exactly. Each must have a `.gitkeep`:

```
backend/api/
backend/models/
backend/utils/
backend/saved_models/
backend/precomputed/
backend/professor_scripts/
```

Run: `mkdir -p backend/api backend/models backend/utils backend/saved_models backend/precomputed backend/professor_scripts`
Then touch `.gitkeep` in each.

Do NOT create any Python files yet — these are empty placeholders only (D-02).
</action>

<acceptance_criteria>
- `backend/api/` exists: `Test-Path "backend/api"` returns True
- `backend/models/` exists: `Test-Path "backend/models"` returns True
- `backend/utils/` exists: `Test-Path "backend/utils"` returns True
- `backend/saved_models/` exists: `Test-Path "backend/saved_models"` returns True
- `backend/precomputed/` exists: `Test-Path "backend/precomputed"` returns True
- `backend/professor_scripts/` exists: `Test-Path "backend/professor_scripts"` returns True
- No Python files exist in any `backend/` subdirectory yet (only `.gitkeep`)
</acceptance_criteria>

---

### Task 3: Create frontend sub-directories
<action>
Create all frontend sub-directories from PRD Section 5 exactly. Each must have a `.gitkeep`:

```
frontend/public/
frontend/src/
frontend/src/api/
frontend/src/context/
frontend/src/pages/
frontend/src/components/
frontend/src/components/layout/
frontend/src/components/shop/
frontend/src/components/recommendations/
frontend/src/components/cart/
frontend/src/components/settings/
frontend/src/hooks/
```

Run:
```
mkdir -p frontend/public frontend/src/api frontend/src/context frontend/src/pages `
  frontend/src/components/layout frontend/src/components/shop `
  frontend/src/components/recommendations frontend/src/components/cart `
  frontend/src/components/settings frontend/src/hooks
```
Then touch `.gitkeep` in each.

Do NOT run `create-react-app` or `npm init` — empty folders only (D-02).
</action>

<acceptance_criteria>
- `frontend/src/components/layout/` exists: `Test-Path "frontend/src/components/layout"` returns True
- `frontend/src/components/shop/` exists: `Test-Path "frontend/src/components/shop"` returns True
- `frontend/src/components/recommendations/` exists: `Test-Path "frontend/src/components/recommendations"` returns True
- `frontend/src/hooks/` exists: `Test-Path "frontend/src/hooks"` returns True
- `frontend/src/pages/` exists: `Test-Path "frontend/src/pages"` returns True
- `frontend/src/api/` exists: `Test-Path "frontend/src/api"` returns True
- No JS/JSX files exist in any frontend directory yet (only `.gitkeep`)
</acceptance_criteria>

---

### Task 4: Move instacart zip to data/raw/
<action>
The file `instacart datset.zip` currently lives in the project root. Move it to `data/raw/`:

```powershell
Move-Item "instacart datset.zip" "data/raw/instacart_dataset.zip"
```

Note: rename it to `instacart_dataset.zip` (removing the typo space) during the move. This is the only file that should be in `data/raw/` at this point.
</action>

<acceptance_criteria>
- `data/raw/instacart_dataset.zip` exists: `Test-Path "data/raw/instacart_dataset.zip"` returns True
- `instacart datset.zip` no longer exists in project root: `Test-Path "instacart datset.zip"` returns False
- `data/raw/` contains exactly 2 files: `instacart_dataset.zip` and `.gitkeep`
</acceptance_criteria>

---

## must_haves

- [ ] All top-level directories from PRD §10 exist
- [ ] All `backend/` sub-directories from PRD §6 exist
- [ ] All `frontend/src/` sub-directories from PRD §5 exist
- [ ] `instacart datset.zip` moved to `data/raw/instacart_dataset.zip`
- [ ] No application code created yet (directories only)

## Verification

Run the following to confirm structure:
```powershell
Get-ChildItem -Recurse -Directory | Select-Object FullName | Where-Object { $_.FullName -notmatch "\.git|\.planning" }
```
Output must include: `data`, `data\raw`, `data\processed`, `notebooks`, `scripts`, `backend`, `backend\api`, `backend\models`, `backend\utils`, `backend\saved_models`, `backend\professor_scripts`, `frontend`, `frontend\public`, `frontend\src`, `frontend\src\components\layout`, `frontend\src\components\shop`, `frontend\src\components\recommendations`, `frontend\src\hooks`
