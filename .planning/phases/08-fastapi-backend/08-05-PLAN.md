---
wave: 4
depends_on: ["08-04-PLAN.md"]
files_modified: ["professor_scripts/run_evaluation.py", "professor_scripts/run_model_comparison.py"]
autonomous: true
requirements: ["PROF-01", "PROF-02"]
---

# Professor Evaluation Scripts

## Context
Two standalone scripts required by University context to evaluate the completed system performance out of band from the web app.

## Verification criteria
- [ ] Scripts run from terminal and do not rely on `uvicorn` backend running
- [ ] Output respects D-04 (prints tables using pandas dataframe formatted tables directly)

## Task 1: Evaluation script
<read_first>
- .planning/phases/08-fastapi-backend/08-CONTEXT.md
</read_first>

<action>
Create `professor_scripts/run_evaluation.py`.
This script should load the X_test dataset from `data/processed/` and the trained `.h5` models + the apriori CSV (`baseline_results.csv`).
Evaluate the exact HR@5, HR@10, Precision@5, MRR metrics over 1,000 users.
Since this evaluates 4 systems, it should construct a python `pandas.DataFrame` and print it natively to standard out.
It should optionally use `matplotlib` to plot `metrics_comparison.png` and save it to the current directory, and also output the table data to `results.csv`.
</action>

<acceptance_criteria>
- `professor_scripts/run_evaluation.py` exists
- Uses `pandas` to format standard terminal output
- Saves both `results.csv` and `metrics_comparison.png`
</acceptance_criteria>

<task_id>01</task_id>

## Task 2: Model Comparison Script
<read_first>
- .planning/phases/08-fastapi-backend/08-CONTEXT.md
</read_first>

<action>
Create `professor_scripts/run_model_comparison.py`.
Allow standard `argparse` CLI inputs for `--cart` (e.g., "43,15,62"), `--hour`, `--dow`.
Load the Apriori model logic strings / lists, and compare it with dynamically running inference via the `lstm_time_model.h5`.
Print the 5 recommendations side-by-side using a Pandas dataframe to the terminal.
</action>

<acceptance_criteria>
- `professor_scripts/run_model_comparison.py` exists
- Uses `argparse` with flags
- Output prints a side-by-side pandas table.
</acceptance_criteria>

<task_id>02</task_id>
