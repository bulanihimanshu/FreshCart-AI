---
wave: 1
depends_on: []
files_modified: ["backend/main.py", "backend/api/health.py", "backend/core/config.py"]
autonomous: true
requirements: ["API-07", "API-08", "API-09"]
---

# Setup Application Core & Model Loading

## Context
Initialize the FastAPI application, set up CORS for React integration, and implement the `lifespan` block to load ML artifacts (models, vocab, lookup tables, and products) cleanly at startup instead of piecemeal.

## Verification criteria
- [ ] `GET /api/health` returns `{"status": "ok"}`
- [ ] Server boot logs indicate LSTM model and JSON tables loaded successfully
- [ ] must_haves:
  - Global app state has pre-loaded model to prevent cold starts
  - CORS allows dev server requests

## Task 1: Create backend/main.py with lifespan
<read_first>
- .planning/phases/08-fastapi-backend/08-CONTEXT.md
- data/precomputed/global_top20.json (if exists)
- vocab.json (if exists)
</read_first>

<action>
Create `backend/main.py`.
Import `FastAPI` and implement the `lifespan` context manager.
Inside lifespan, load the following into `app.state` globally:
- `vocab`: load from `vocab.json`
- `lstm_time_model`: load from `saved_models/lstm_time_model.h5`
- `global_top20`, `hourly_top10`, `dow_top10`, `aisle_top10`: load from `data/precomputed/`
- `products_df`: load `products.csv` (if it exists, using pandas or csv) and build a dict mapping `product_id` -> `{name, aisle, department}` to use for hydration (D-05).
Set up `CORSMiddleware` globally, `allow_origins=["*"]` (or explicitly `"http://localhost:5173"`, `"http://localhost:3000"`), `allow_methods=["*"]`, `allow_headers=["*"]`.
</action>

<acceptance_criteria>
- `backend/main.py` contains `async def lifespan(app: FastAPI):`
- `backend/main.py` contains `app.add_middleware(CORSMiddleware`
- Model is loaded via `tf.keras.models.load_model` inside lifespan
</acceptance_criteria>

<task_id>01</task_id>

## Task 2: Implement Health Endpoint
<read_first>
- backend/main.py
</read_first>

<action>
Create `backend/api/health.py`.
Create an `APIRouter()` named `router`.
Implement `GET /api/health` that returns `{"status": "ok"}`.
Include this router in `backend/main.py` via `app.include_router(health.router)`.
</action>

<acceptance_criteria>
- `backend/api/health.py` contains `@router.get("/api/health")`
- `backend/main.py` contains `app.include_router(health.router)`
- curl /api/health returns `{"status": "ok"}`
</acceptance_criteria>

<task_id>02</task_id>
