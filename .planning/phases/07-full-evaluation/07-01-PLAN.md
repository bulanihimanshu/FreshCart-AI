---
wave: 1
depends_on: []
files_modified: ["notebooks/07_Evaluation.ipynb"]
autonomous: true
requirements: ["ML-07"]
---

# 07-01: Test Harness & Full System Simulation

<objective>
Build the unified evaluation pipeline in `07_Evaluation.ipynb`. Define the harness to orchestrate predictions for all 4 systems (Apriori, LSTM, LSTM+Time, Full System) against the 1000 held-out test users. Implement the dynamic sequence truncation function to simulate the 3-tier cold start distribution in memory.
</objective>

<tasks>
  <task>
    <description>Initialize notebook and load all required dependencies and models</description>
    <read_first>
      - notebooks/05_Time_Features.ipynb
    </read_first>
    <action>
      Create `notebooks/07_Evaluation.ipynb`. 
      Import `numpy`, `pandas`, `json`, `tensorflow as tf`, `matplotlib.pyplot`.
      Write code to load:
      1. Data: `data/processed/X_test.npz`, `data/processed/vocab.json`.
      2. Models: `saved_models/lstm_model.h5`, `saved_models/lstm_time_model.keras`.
      3. Precomputed tables: `data/precomputed/global_top20.json`, `data/precomputed/hourly_top10.json`, `data/precomputed/dow_top10.json`, `data/precomputed/aisle_top10.json`.
      4. `data/processed/baseline_results.csv` (for Apriori metrics).
    </action>
    <acceptance_criteria>
      - Notebook contains markdown headers for "1. Setup & Data Loading".
      - Code cell defines `np.load()`, `keras.models.load_model()`, and `json.load()` calls.
    </acceptance_criteria>
  </task>

  <task>
    <description>Implement dynamic dataset truncation for Full System</description>
    <read_first>
      - .planning/phases/07-full-evaluation/07-CONTEXT.md
    </read_first>
    <action>
      Define a function `create_simulated_test_set(X_test, random_seed=42)` that:
      1. Takes the array of test users.
      2. Shuffles or loops over them using the random seed.
      3. Forces a precise distribution: 20% lengths = 0 (Tier 1), 30% lengths = 1 or 2 randomly (Tier 2), 50% unmodified lengths (Tier 3).
      4. Replaces the history elements with padding (0) for the truncated items.
      5. Returns this new memory-only array as `X_test_simulated`.
    </action>
    <acceptance_criteria>
      - Code block defines `create_simulated_test_set`.
      - Assertions verify that exactly 20% of rows contain only padding, 30% contain <= 2 non-padding items, and 50% match original `X_test`.
    </acceptance_criteria>
  </task>

  <task>
    <description>Port the 3-tier routing logic into the evaluation harness</description>
    <read_first>
      - notebooks/06_Cold_Start.ipynb
    </read_first>
    <action>
      Re-implement the `get_user_tier(order_count)` and unified `get_recommendations()` router function inside the notebook so it can iterate over `X_test_simulated`. Ensure that for Tier 3, it correctly calls the `lstm_time_model.keras` instead of failing. Use synthetic time variables (e.g., hour=14, dow=3, days_gap=7) for uniform testing or reuse the synthetic time features extracted from the test labels if available.
    </action>
    <acceptance_criteria>
      - Router clearly maps order_count 0 -> Tier 1, 1-2 -> Tier 2, 3+ -> Tier 3.
      - Function returns exactly an array of 5 product IDs for any given user row.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run the first section of `07_Evaluation.ipynb`. Verify `X_test_simulated` length and sequence distributions meet the 20/30/50 ratio.
</verification>

<must_haves>
- X_test must not be saved/overwritten to disk.
- Router function must handle empty sequences safely out-of-the-box.
</must_haves>
