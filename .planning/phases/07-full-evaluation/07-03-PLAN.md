---
wave: 3
depends_on: ["07-02"]
files_modified: ["notebooks/07_Evaluation.ipynb"]
autonomous: true
requirements: ["ML-07"]
---

# 07-03: Visualization & Charting

<objective>
Generate a grouped bar chart (`metrics_comparison.png`) that visually compares the 4 systems across the evaluated metrics, structured perfectly by metric cluster as approved in the user context.
</objective>

<tasks>
  <task>
    <description>Generate grouped bar chart for evaluating the 4 systems</description>
    <action>
      In `07_Evaluation.ipynb`, use matplotlib and seaborn.
      Load `results.csv` into a Pandas DataFrame `df`.
      Drop the 'Coverage (%)' column from the bar chart to avoid axis scale distortion (Coverage represents a percentage from 0-100, while HR/Precision/MRR are 0-1 ratios. Plotting them on the same axis ruins the visualization).
      Melt the remaining precision/recall metrics so the DataFrame format is suitable for a grouped bar chart (`seaborn.barplot` with `hue='System'`).
      Set plot style to `seaborn-darkgrid` (or equivalent).
      Create a clustered bar plot where the X axis represents the Metrics (HR@5, HR@10, Precision@5, MRR), and the grouped bars represent the 4 systems.
      Add specific bar value annotations on top of the bars to make reading the chart easier.
    </action>
    <acceptance_criteria>
      - Plotting logic exists for matplotlib/seaborn.
      - Chart has X-axis = Metrics, Hue = System.
      - Chart specifically excludes 'Coverage (%)' from the 0-1 scale axis to prevent visual breakage.
    </acceptance_criteria>
  </task>

  <task>
    <description>Save the visualization output</description>
    <action>
      Save the generated figure to `data/processed/metrics_comparison.png` using `plt.savefig()` with a DPI of 300 and `bbox_inches='tight'`.
    </action>
    <acceptance_criteria>
      - `plt.savefig('data/processed/metrics_comparison.png')` is called.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Ensure the image file `data/processed/metrics_comparison.png` is generated without error.
</verification>

<must_haves>
- The bar chart must cleanly demonstrate the progressive improvement of Apriori -> LSTM -> LSTM+Time -> Full System.
</must_haves>
